BMI Calculator:
- Users enter their weight(kg) and height(m).
- Error handling for invalid inputs
- The program calculates the users BMI and assign a category to them.
- The results are then displayed to the user.
- Use a graphical user interface (GUI)
